import { toggleFilterContainer } from '../components/Queries/FilterContainer';

export const clientMutations = {
    toggleFilterContainer,
};

export const clientDefaults = {
    filtersOpen: false,
};