overwrite: true
schema: "http://localhost:4000/graphql"
documents: "graphql/server/**/*.ts"
generates:
  generated/apolloComponents.tsx:
    config:
      noNamespaces: true
    plugins:
      - "typescript-common"
      - "typescript-client"
      - "typescript-react-apollo"
